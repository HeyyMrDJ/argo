apiVersion: v1
kind: Service
metadata:
  name: argocd-server-lb
  namespace: argocd
spec:
  type: LoadBalancer
  ports:
    - name: https
      port: 6901
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: argocd-server
